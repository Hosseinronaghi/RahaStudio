# اصلاحات نسخه 0.3.1

این نسخه خطاهای گزارش‌شده توسط GitHub Actions را اصلاح می‌کند:

- جایگزینی `value` با `initialValue` در `DropdownButtonFormField`
- حذف مسیرهای Asset خالی از `pubspec.yaml`
- جایگزینی تست پیش‌فرض Flutter که به کلاس ناموجود `MyApp` اشاره می‌کرد
- افزایش نسخه به `0.3.1+4`

پس از جایگزینی فایل‌ها، Workflow را دوباره اجرا کنید.
